# prompt-color

```
Commands

$ cd /home/$USER
$ curl -o ~/.git-prompt.sh     https://raw.githubusercontent.com/git/git/master/contrib/completion/git-prompt.sh
$ echo "source ~/.git-prompt.sh" >> .bashrc
$ source .bashrc

One Command
$ cd /home/$USER && curl -o ~/.git-prompt.sh https://raw.githubusercontent.com/git/git/master/contrib/completion/git-prompt.sh && echo "source ~/.git-prompt.sh" >> .bashrc && source .bashrc
```
